<Page
    x:Class="DiceGame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DiceGame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel>
            <ComboBox IsEditable="True" 
                      ItemsSource="{x:Bind NumberOfDiceSource}" 
                      Text="2"
                      HorizontalAlignment="Right"
                      x:Name="ComboBox" />
            <local:DicePanel x:Name="Dices" NumberOfDice="{x:Bind ComboBox.Text ,Mode=OneWay}" />

            <RepeatButton Content="Throw" Click="Roll" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Page>
