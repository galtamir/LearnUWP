<Window x:Class="GameUI_Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameUI_Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="GameBoard">
            
        </Grid>
        <Button Grid.Row="1"  Content="New Game" Click="CreateNewGame"/>
        <StackPanel Grid.Column="1" x:Name="Settings">
            <TextBlock Text="Settings" FontSize="42" HorizontalAlignment="Center" />
            <TextBlock/>
            <TextBlock Text="Heigth" Margin="10"/>
            <Slider x:Name="HeigthSlider" Minimum="10" Maximum="1000" Margin="10"/>
            <TextBlock Text="Width" Margin="10"/>
            <Slider x:Name="WidthSlider" Minimum="10" Maximum="1000" Margin="10"/>
            <TextBlock/>
            <CheckBox x:Name="CyclicCheckBox" Content="Is Cyclic" Margin="10" IsChecked="True"/>
        </StackPanel>
    </Grid>
</Window>
